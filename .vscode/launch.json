{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch User Service",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/user-service/cmd/server",
            "cwd": "${workspaceFolder}/user-service"
        }
    ]
}
